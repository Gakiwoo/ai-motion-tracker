# 2026-04-25 日志

## 修复 MediaPipe CDN 为国内镜像
- 将两端（桌面端 + 手机端）所有 `cdn.jsdelivr.net/npm/@mediapipe/...` 替换为 `registry.npmmirror.com/@mediapipe/.../files/...`
- 同步更新 `tauri.conf.json` 的 CSP 白名单域名

## 桌面端 TS 编译错误修复（打包前置）
修复 6 处 TypeScript 编译错误：
1. `mediapipe.d.ts`：全局 `Pose` 接口重命名为 `MediaPipePose`（与 types/index.ts 的 Pose 类型冲突）
2. `CameraView.tsx`：`poseRef` 类型改为 `MediaPipePose`
3. `AnalyticsPage.tsx`：`getDayLabels` 返回类型 `string[]` → `{ dateStr: string; label: string }[]`
4. `WorkoutPage.tsx`：移除未使用的 `WorkoutMode`/`DEFAULT_DURATIONS` import + 删除死代码 `handleSetTarget`
5. `SitUpCounter.ts`：import 路径 `'../types'` → `'../../types'`；删除未使用字段 `directionChangeCount`/`currentTrunkAngle`/`avgCycleFrames`/`shoulderMidY`；`handleLying(hipY)` → `_hipY`

## 桌面端打包 EXE 成功
- 打包类型：NSIS 安装包（MSI 模式的 WiX light.exe 工具异常，改用 nsis）
- 产物：`src-tauri/target/release/bundle/nsis/AI运动助手_1.0.0_x64-setup.exe`（2MB）
- `tauri.conf.json` 的 `targets` 已改为 `["nsis"]`

## APK 打包（EAS 云端构建）成功提交
- Expo 账号：gakiwoo7@gmail.com（用户名 gakiwoo1006）
- EAS 项目已初始化：`@gakiwoo1006/ai-motion-tracker`（ID: 65ffbfbe-0bfe-4a4b-8eee-79ecf40bdd15）
- Account ID: 7e750444-c101-4df7-b8b7-56b6ced0efeb
- Keystore 通过 Expo GraphQL API 创建（keytool 本地生成 + API 上传），因为 IDE 终端不支持交互式 stdin
- EXPO_TOKEN: `iVS38HkCFnpX0ntnI2ExHU4XQsULCV-PWXfPfqtM`
- Git 仓库已初始化（EAS 要求）
- `eas.json` 配置：preview profile 用 `buildType: "apk"`，production 用 `"app-bundle"`
- 构建日志：https://expo.dev/accounts/gakiwoo1006/projects/ai-motion-tracker/builds/5c2d68ea-9500-48fc-9d02-8886b945cafc

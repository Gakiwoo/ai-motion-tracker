## ai-sport-desktop 优化批次 (2026-04-24 下午)

### 已完成
- **P0-1**: AnalyticsPage 图表深色模式 — CartesianGrid/Tooltip/XAxis/YAxis 颜色动态切换
- **P0-2**: SitUpCounter setTimeout 内存泄漏 — doneTimerId 类属性 + reset() 中 clearTimeout
- **P0-3**: WorkoutPage 弹窗 background 改用 var(--bg-card)，深色模式自动适配
- **P1-5**: 统一 EXERCISE_COLORS + EXERCISE_CARD_THEMES 到 exerciseConfig.ts，消除 3 处重复定义
- **P1-6**: VALID_EXERCISE_TYPES / ALL_TYPES 从 EXERCISE_CONFIGS 派生
- **P1-7**: StorageService.getWorkoutHistory 添加 isValidSession 校验层
- **P1-9**: CameraView Pose.score 改用实际关键点置信度均值
- **P2**: 新增 ErrorBoundary 组件，包裹 App 根
- **P2**: useWorkout 中 crypto.randomUUID 添加 generateId fallback

### 第二轮优化（2026-04-24 晚）
- **data-progress 浮点数**：Math.round(progress) 修复 CSS 属性选择器永远不匹配的 bug
- **死 CSS 清理**：`.progress-fill[data-complete]` → `.progress-fill--complete` class + JS 条件
- **死 import**：useWorkout 移除未使用的 playGoalReached
- **死依赖**：npm uninstall @mediapipe/drawing_utils（全项目零引用）
- **深色模式 WorkoutPage**：modal-input background/focus 适配
- **深色模式 global.css**：滚动条 thumb + btn-ghost:hover 适配
- **MediaPipe 类型声明**：新增 src/types/mediapipe.d.ts，消除 CameraView 6 处 eslint-disable

### 第三轮优化：算法准确性修复（2026-04-24 晚）
- **PeakDetector 外部基线**：detect() 添加可选 `externalBaseline` 参数，支持传入 baselineWindow 中值
- **原地纵跳 V3**：添加 90 帧 baselineWindow + 动态基线更新（峰值后 15 帧稳定才更新）+ externalBaseline + minHeight 0.02→0.025
- **仰卧起坐 lying→rising 确认帧**：新增 CONFIRM_FRAMES_LYING_TO_RISING=3，角度<120° 需持续 3 帧才触发 rising
- **仰卧起坐 setTimeout→帧计数器**：移除 doneTimerId + setTimeout，done 阶段 6 帧（~200ms）冷却后回到 lying
- **立定跳远 V3**：峰值检测传入 baselineWindow 中值 + 仅峰值后 20 帧稳定时更新基线

### 修改文件清单
- `src/constants/exerciseConfig.ts` — 新增 EXERCISE_COLORS、EXERCISE_CARD_THEMES
- `src/pages/AnalyticsPage.tsx` — 深色模式图表配色、移除本地 EXERCISE_COLORS
- `src/pages/HistoryPage.tsx` — 引用统一常量、从 EXERCISE_CONFIGS 派生 ALL_TYPES
- `src/pages/HomePage.tsx` — 引用 EXERCISE_CARD_THEMES、移除本地 cardThemes
- `src/pages/WorkoutPage.tsx` — 引用统一常量、从 EXERCISE_CONFIGS 派生白名单
- `src/pages/WorkoutPage.css` — modal-box 改用 var(--bg-card)
- `src/services/StorageService.ts` — 添加 isValidSession 校验
- `src/services/counters/SitUpCounter.ts` — 修复 setTimeout 泄漏
- `src/components/CameraView.tsx` — Pose.score 动态计算
- `src/hooks/useWorkout.ts` — generateId fallback
- `src/components/ErrorBoundary.tsx` — 新增
- `src/App.tsx` — 包裹 ErrorBoundary

### 双模式功能开发（2026-04-24 深夜）
- **两端同步**：手机端 + 桌面端均新增定时计数模式（WorkoutMode = 'count' | 'timed'）
- **手机端改动的文件**：
  - `src/types/index.ts` — 新增 WorkoutMode 类型 + WorkoutSession.mode 字段
  - `src/constants/exerciseConfig.ts` — 新增 DEFAULT_DURATIONS 默认时长
  - `src/hooks/useWorkout.ts` — 完整重写，双模式状态管理 + 定时器轮询 + timeUp 自动停止
  - `src/services/StorageService.ts` — getWorkoutHistory 向后兼容 mode 字段 + getAnalytics 增加 totalDuration
  - `src/screens/WorkoutScreen.tsx` — 完整重写，模式切换器 + 倒计时 + 快速时长选择 + 停止确认上下文化
  - `src/screens/HistoryScreen.tsx` — 卡片新增模式标签（🎯定数 / ⏰定时）+ 用时显示
  - `src/screens/AnalyticsScreen.tsx` — 新增累计用时统计行
- **编译验证**：手机端 tsc --noEmit 零错误 ✅

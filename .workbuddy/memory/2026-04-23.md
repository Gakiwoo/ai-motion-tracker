# 2026-04-23

## 算法重构完成（7 项任务）

### P1 级别（核心算法）
1. **P1-1 抽离共享工具类** → 新建 `src/utils/filters.ts`，导出 `KalmanFilter1D` + `SlidingWindow`，3 个 Counter 改为 import，消除重复代码
2. **P1-2 跳绳算法重写 V2** → 从 42 行扩到 ~400 行。新增手腕旋转周期检测、双信号融合（手腕+髋部）、Kalman 滤波、4 阶段状态机（idle/detecting/jumping/resting）、自适应节奏、反馈系统
3. **P1-3 深蹲算法升级 V2** → 从 26 行扩到 ~360 行。新增 Kalman 滤波、5 阶段状态机（idle/standing/descending/bottom/ascending）、犯规检测（背部前倾/蹲深不够/膝盖内扣）、自适应标定、反馈系统
4. **P1-4 开合跳算法升级 V2** → 从 46 行扩到 ~300 行。新增 Kalman 滤波、5 阶段状态机（idle/closed/opening/open/closing）、自适应阈值（基于肩宽/髋宽）、防抖确认
5. **P1-5 纵跳算法修复** → 修正高度计算：从 `crouchAnkleY`（蹲底参考）改为 `takeoffAnkleY`（起跳瞬间参考），避免蹲深污染跳跃高度

### P2 级别（架构优化）
6. **P2-1 WebView 帧率优化** → 从 300ms（~3fps）降到默认 100ms（~10fps），支持通过 `throttleMs` prop 动态配置，支持运行时通过 postMessage 调整
7. **P2-2 统一基类接口** → `ExerciseCounter` 新增 `CounterFeedback` 类型、`getPhase()` 默认实现、`getFeedback()` 默认实现（返回 null）

### 文件变更清单
- 新增: `src/utils/filters.ts`
- 修改: 6 个 Counter 文件 + `ExerciseCounter.ts` + `CameraView.tsx`
- 全部 lint 通过，0 错误

## 算法评审 & Bug 修复（8 项）

### P0 级别（实际 Bug）
1. **深蹲 hipMidX typo** — `rightHip.y` → `rightHip.x`，导致背部角度计算错误
2. **跳绳交叉验证过于严格** — 手腕周期和弹跳要求同帧匹配导致漏计。改为 `bounceWindow`（60帧）回溯窗口，过去 N 帧内有弹跳即可
3. **跳绳峰检测缺陷** — 局部 10 帧均值不稳定 + 无最小峰间隔。改为全窗口均值 + `MIN_CYCLE_FRAMES` 间隔防噪声
4. **开合跳 handleClosed 误触发** — `||` 改 `&&`，要求手臂和腿部同时开始动作才触发

### P1 级别（体验优化）
5. **全局 getRate 硬编码 30fps** — 基类添加 `frameIntervalMs`(默认100ms) + `totalFrames` + 通用 `getRate()`。4 个子类删除各自实现
6. **仰卧起坐死代码** — 删除从未被调用的 `checkElbowKneeTouch` 方法（~22行）
7. **深蹲犯规记录丢失** — `recordSquat` 中蹲深不够时保留 `lastFoul='shallow_squat'`，UI 可正确显示

### tsconfig 修复
8. 精简 `tsconfig.json`，删除与 `expo/tsconfig.base` 冲突的 `module/target/lib/esModuleInterop` 等
